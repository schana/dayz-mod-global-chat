modded class ChatLine {
    private const int GAME_TEXT_COLOUR = ARGB (255, 0xff, 0xd5, 0x4f);

    void ChatLine (Widget root_widget) {
        m_NameWidget.SetShadow (10);
        m_TextWidget.SetShadow (10);
    }
}